# Copy to .env — never commit .env.
# Only EXPO_PUBLIC_* values reach the app bundle, and everything in the bundle is public.
EXPO_PUBLIC_SUPABASE_URL=https://buodsskyppiugzybkihu.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_SENTRY_DSN=

# Build-time only (EAS / local scripts). Not shipped.
SENTRY_ORG=
SENTRY_PROJECT=

# Google / Drive credentials are NOT here on purpose.
# They exist only as Supabase Edge Function secrets (supabase secrets set ...).
